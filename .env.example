# Example environment settings. Both are optional — the app has working
# defaults for each. Set them as real environment variables when starting the
# server; the app does not read a .env file itself.
#
#   PORT=3000                 # HTTP port (default 3000)
#   DB_FILE=data/courses.db   # SQLite file location (default data/courses.db)
